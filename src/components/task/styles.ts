import { StyleSheet } from 'react-native'

export const stylesTask = StyleSheet.create({
  containerForm: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    width: '80%',
    gap: 15,
  },
})